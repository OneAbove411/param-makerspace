# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables — never commit real keys
.env
.env.*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Build artifacts (temporary build variants — should not be tracked)
dist-tmp
dist-tmp2
dist-verify

# Credentials / secrets — never commit
migrate-pass.txt
*.secret
*.key
*.pem

# Internal tooling scripts
scripts/

# Claude / Cowork session data
.claude/
